<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.repository.UserMapper">
    <select id="search" resultType="com.example.demo.entity.User">
    	select id,name,department,gender,age from user where( age between #{agestart} and #{ageend}) AND gender=#{gender};
    </select>
    
    
    <select id="path"  resultType="String">
    	select path from user where( age between #{agestart} and #{ageend}) AND gender=#{gender};
    </select>
    
</mapper>
